import React from 'react';

const SearchForm = () => (
    <form>
        <label>
            <input type="text" />
        </label>
        <button type="submit">Поиск</button>
    </form>
);

export default SearchForm;
